version: "3.9"

# Experimental agent API in the default cloud mode.

services:
  rhaef-api:
    build: .
    container_name: rhaef-v2-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
